정답: ③ (3번)

해설:
연결 지향 서비스에서 상대 프로세스가 보낸 데이터를 읽는 함수는 recv()다.

각 보기 분석:
① connect()는 연결 요청이다.
② listen()은 대기 상태 설정이다.
③ recv()는 수신 함수다. 정답이다.
④ sendto()는 비연결성 송신 함수다.

핵심 정리:
TCP 수신은 recv(), UDP 송신은 sendto()를 쓴다.
