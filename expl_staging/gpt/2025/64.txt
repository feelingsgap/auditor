정답: ③ (3번)

해설:
확장성 해싱에서 지정된 세 모조키를 삭제한 뒤의 상태를 묻는 문제다.

삭제 후 버킷 병합과 디렉터리 축소를 적용하면:
- 전역 깊이는 2로 줄어든다.
- 지역 깊이의 합은 8이 된다.
- 가장 아래 버킷에는 `110100` 하나만 남는다.
- 3개의 모조키를 가진 버킷은 존재하지 않는다.

따라서 옳지 않은 설명은 ③이다.

핵심 정리:
삭제 후 합병이 일어나면 버킷 수와 전역 깊이가 줄어들 수 있다.
