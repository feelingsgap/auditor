정답: ① (1번)

해설:
진리표를 보면 `A=0`일 때는 항상 `F=1`이고, `A=1`일 때는 `B=C=1`일 때만 `F=1`이다.

따라서 식은:
- `A'=1`이면 항상 출력 1
- `A=1`일 때는 `BC=1`이어야 1

이를 합치면 `F = A' + BC`

각 보기 분석:
① `A' + BC` — 최소화된 식으로 정답이다.
②, ③ — 같은 의미를 더 복잡하게 쓴 식이다.
④ — 더 비최소화된 식이다.

핵심 정리:
카르노 맵으로 묶으면 `A'`와 `BC` 두 항으로 최소화된다.
