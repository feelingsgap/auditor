정답: ① (1번)

해설:
CRC 하드웨어 구성으로부터 생성다항식을 찾는 문제다.

시프트 레지스터 수와 XOR 연결 위치를 보면 최고차항은 `x^4`이고, 중간 탭이 `x^2`에 해당하며 상수항 `1`이 포함된다.
따라서 생성다항식은 `x^4 + x^2 + 1`이다.

정답은 ①이다.

핵심 정리:
CRC 회로는 레지스터 단계와 XOR 탭 위치를 보고 생성다항식으로 옮긴다.
