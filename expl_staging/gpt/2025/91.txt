정답: ② (2번)

해설:
POP3 서비스 명령을 묻는 문제다.

각 보기 분석:
① `HELO` — SMTP 명령이다.
② `LIST + <일련번호>` — POP3에서 메일 목록이나 특정 메일 정보를 조회할 때 쓰는 명령이다. 정답이다.
③ `MAIL` — SMTP의 발신자 지정 명령이다.
④ `RCPT` — SMTP의 수신자 지정 명령이다.

핵심 정리:
POP3는 수신용 프로토콜이고, `LIST`, `RETR`, `DELE` 같은 명령을 사용한다.
