정답: ③ (3번)

해설:
OID는 객체를 유일하게 식별하는 시스템 생성 식별자다.
관계 데이터 모델의 기본키와 본질적으로 같은 성질이라는 설명은 틀리다.

각 보기 분석:
① 객체마다 유일하다 - 맞다.
② 생성 후 변경되지 않는다 - 맞다.
③ 기본키와 본질적으로 동일하다 - 틀리다. 정답이다.
④ 객체 삭제 시 삭제되고 재사용되지 않는다 - 맞다.

핵심 정리:
OID는 값 기반이 아니라 객체 자체를 식별하는 불변 ID다.
