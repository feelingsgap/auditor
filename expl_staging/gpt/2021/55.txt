정답: ② (2번)

해설:
뷰는 삽입이 거부될 수 있고, 집계 함수가 포함된 뷰는 일반적으로 갱신이 허용되지 않는다.
GROUP BY를 포함한 뷰를 생성하는 것은 가능하다.

각 보기 분석:
가 - 맞다.
나 - 틀리다. GROUP BY를 사용할 수 있다.
다 - 맞다.
라 - 틀리다. 일반 뷰는 CREATE VIEW, 실체화 뷰는 별도 구문을 사용한다.

핵심 정리:
갱신 가능성은 뷰의 구성 요소에 따라 제한된다.
