정답: ④ (4번)

해설:
2의 보수 덧셈에서 오버플로 플래그는 최상위 캐리와 그 직전 캐리의 XOR로 구한다. 따라서 `C3 ⊕ C4`가 정답이다.

각 보기 분석:
① C3 + C4 - 논리식이 아니다.
② C3 · C4 - 오버플로 조건을 완전히 표현하지 못한다.
③ ¬C3 + ¬C4 - 관련이 없다.
④ C3 ⊕ C4 - 정답이다.

핵심 정리:
부호 있는 덧셈의 오버플로는 캐리의 XOR로 판정한다.
