정답: ② (2번)

해설:
외부 IP에서 내부 FTP 서버 21번 포트로 들어오는 트래픽을 탐지하고 차단하려면 `drop tcp !192.168.0.0/24 any -> 192.168.0.0/24 21` 규칙이 적절하다.

각 보기 분석:
① alert만 하고 차단하지 않는다.
② 탐지와 차단을 함께 수행하는 규칙이다. 정답이다.
③ 내부에서 외부로 가는 방향이라 반대다.
④ 방향과 action이 맞지 않는다.

핵심 정리:
Snort에서 차단은 `drop`, 탐지는 `alert`를 쓴다.
